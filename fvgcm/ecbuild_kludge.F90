! ecbuild_add_library() does not support empty source list.  Actually,
! it might, but if so, another argument needs to be passed down from
! the local CMakeLists.txt file.

subroutine ecbuild_kludge()
end subroutine ecbuild_kludge
